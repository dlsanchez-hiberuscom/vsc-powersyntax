# Quickstart - Spec 251 Corpus enterprise OrderEntry baseline (B226)

```bash
npm run test:performance -- --grep "OrderEntry"
```

El corpus es local y vive fuera de Git en `fixtures-local/STD_FC_OrderEntry`.
