# Tasks — Spec 404 / B309

- [x] Reutilizar discovery para registrar metadata/policy files SCM y outputs locales sin tratarlos como roots ni source.
- [x] Proyectar esa señal en `workspaceMigrationAssistant` con recomendaciones read-only sobre governance y ruido local.
- [x] Validar el carril `discover -> state -> migration assistant` con fixtures de `.git`, `.svn`, `.scc`, `.gitignore`, `.gitattributes`, `.pb`, `build` y `_backupfiles`.
- [x] Alinear backlog, current-focus, roadmap, done-log y workflow de migración con el cierre del slice.