# Tasks — Spec 403 / B307

- [x] Reutilizar `datatype` y `baseTypeName` ya indexados para contar superficies `WebBrowser` en `code metrics`.
- [x] Publicar un hotspot `web-ui-integration` en `technical-debt-report` con superficies, patrones defendibles y `web-ui-risk:no-content-inspection`.
- [x] Reutilizar esa misma evidencia visible en `workspaceCheckReport`, Markdown y support bundle a través del debt report saneado.
- [x] Versionar el contrato público aditivo y validar el slice con `powerBuilderCodeMetrics`, `powerBuilderTechnicalDebtReport`, `workspaceCheckReport`, `supportBundle` y `publicApi`.