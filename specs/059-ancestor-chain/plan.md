# Plan 059 (B065)
- `features/ancestorNav.ts` + tests.
- `features/hierarchyInspection.ts` sobre `InheritanceGraph` y `member closures`.
- wiring en `server.ts` y `client/extension.ts` para inspección jerárquica activa.
