# Plan 053 (B063)
- `features/diagnosticsSnapshot.ts` como builder único del snapshot agrupado.
- `features/diagnostics.ts` como consumer/export del mismo contrato.
- tests unitarios sobre builder y publish/export path.
