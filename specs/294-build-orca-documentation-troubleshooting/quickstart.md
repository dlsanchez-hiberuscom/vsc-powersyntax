# Quickstart - Spec 294 Build/ORCA documentation and troubleshooting (B198)

```bash
npm run build:test
```

Comprobación documental mínima:

1. Verificar que `README.md` y `docs/developer-workflows.md` citan los comandos visibles de `package.json` para `PBAutoBuild` y `ORCA legacy`.
2. Verificar que las rutas `tools/pbautobuild-ci`, `.vsc-powersyntax/orca-export/*` y `.vsc-powersyntax/runtime/build-orca-journal.json` aparecen descritas con el mismo significado que en el runtime actual.
3. Verificar que backlog/current-focus/roadmap/done-log ya no tratan `B198` como deuda activa.
