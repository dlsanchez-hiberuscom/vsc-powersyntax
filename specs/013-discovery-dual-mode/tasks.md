# [013] Tasks — Discovery Dual Mode (B120)

## Implementación
- [x] T1. Ampliar `WorkspaceRoots` con `solutions` y `projects` en `workspaceState.ts`.
- [x] T2. Añadir `WorkspaceState.getMode()`.
- [x] T3. Detectar `.pbsln` / `.pbproj` en `discovery.ts`.
- [x] T4. Excluir `.pb`, `build`, `_backupfiles` en `IGNORED_DIRECTORIES`.

## Tests
- [x] T5. Tests para los 4 escenarios de `getMode()`.
- [x] T6. Test que verifica exclusión de directorios ruido.

## Documentación
- [x] T7. Marcar B120 como cerrada en `current-focus.md` y `backlog.md`.
- [x] T8. Mencionar modo Dual en `README.md`.
