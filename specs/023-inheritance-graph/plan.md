# Plan — 023 InheritanceGraph (B058)

- Métodos nuevos: `getDirectDescendants`, `getDescendants`, `isDescendantOf`.
- Caché `descendantCache: Map<string, string[]>`.
- Test `test/server/unit/inheritanceGraph.descendants.test.ts`.
