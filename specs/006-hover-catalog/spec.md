# Spec 006 - Catálogo oficial y hover enriquecido

**Estado:** cerrada históricamente y normalizada por B233.

## Resumen

Esta spec introdujo el `SystemCatalog` y el primer hover enriquecido basado en catálogo oficial + `KnowledgeBase`, creando la base que luego se amplió en specs posteriores de hover y runtime catalog.

## Evidencia actual en el repo

- `src/server/knowledge/system/SystemCatalog.ts`
- `test/server/unit/systemCatalog.test.ts`
- `test/server/unit/hover.test.ts`

## Validación histórica relevante

- `test/server/unit/systemCatalog.test.ts`
- `test/server/unit/hover.test.ts`