# Spec 007 - Document Symbols jerárquicos y scopes base

**Estado:** cerrada históricamente y normalizada por B233.

## Resumen

Esta spec fijó la primera versión de `Document Symbols` jerárquicos y del campo `containerName` en el modelo semántico, preparando el camino para el outline moderno y sus reconciliaciones posteriores.

## Evidencia actual en el repo

- `src/server/features/documentSymbols.ts`
- `test/server/unit/documentSymbols.test.ts`
- enriquecimientos posteriores en `specs/256-parser-symbol-lsp-reconciliation`.

## Validación histórica relevante

- `test/server/unit/documentSymbols.test.ts`