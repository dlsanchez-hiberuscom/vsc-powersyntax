# Quickstart - Spec 262 PBAutoBuild build profiles, commands and status UX (B185)

```bash
npm run build:test
npx mocha --ui tdd out/test/server/unit/statusBarPresentation.test.js
npm run test:smoke -- --grep "registra comandos de PBAutoBuild"
```