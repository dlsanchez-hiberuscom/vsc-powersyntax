# Tasks — Spec 400 / B310

- [x] Proyectar warnings lifecycle existentes como hotspot `lifecycle-risk` en `technical-debt-report`.
- [x] Publicar evidencia defendible por `missing-super`, `missing-trigger` y hooks no resueltos sin abrir un motor nuevo.
- [x] Reutilizar `lifecycleWarnings` de `code metrics` y reflejar el resultado en el Markdown visible del informe.
- [x] Validar el slice con `diagnostics`, `powerBuilderCodeMetrics`, `powerBuilderTechnicalDebtReport` y `publicApi`.