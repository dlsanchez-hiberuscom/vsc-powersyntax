# Tasks — Spec 401 / B308

- [x] Reutilizar external functions y `native-dependency` para desglosar `external-dependency` por `dll`, `pbx` y `unknown`, con aliases y consumers visibles.
- [x] Publicar riesgo operativo y build/ORCA impact defendibles sin cargar binarios nativos ni abrir un segundo motor.
- [x] Reutilizar esa misma evidencia en `workspaceCheckReport` y mantenerla visible en support bundle mediante el debt report saneado.
- [x] Validar el slice con `externalFunctions`, `powerBuilderTechnicalDebtReport`, `workspaceCheckReport` y `supportBundle`.
