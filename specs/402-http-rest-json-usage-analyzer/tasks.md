# Tasks — Spec 402 / B306

- [x] Reutilizar `datatype` y `baseTypeName` ya indexados para contar usos HTTP/REST y JSON en `code metrics`.
- [x] Publicar un hotspot `modern-integration` en `technical-debt-report` con superficies, patrones y endpoints redactados por defecto.
- [x] Reutilizar esa misma evidencia visible en `workspaceCheckReport` y mantenerla intacta en support bundle a través del debt report saneado.
- [x] Versionar el contrato público aditivo y validar el slice con `powerBuilderCodeMetrics`, `powerBuilderTechnicalDebtReport`, `workspaceCheckReport`, `supportBundle` y `publicApi`.